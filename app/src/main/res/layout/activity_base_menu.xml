<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/baseRootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipChildren="false"
    android:clipToPadding="false">

    <!-- Contenedor donde las activities hijas ponen su contenido -->
    <FrameLayout
        android:id="@+id/contentContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Overlay oscuro cuando el menu esta abierto -->
    <View
        android:id="@+id/fabOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#80000000"
        android:visibility="gone"
        android:alpha="0"
        android:clickable="true"
        android:focusable="true" />

    <!-- Mini FAB: Home -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabHome"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="22dp"
        android:layout_marginEnd="22dp"
        android:contentDescription="@string/fab_home"
        android:src="@drawable/ic_home"
        android:visibility="invisible"
        app:fabSize="mini"
        app:backgroundTint="@color/btnSecundario"
        app:tint="@color/white"
        app:elevation="6dp" />

    <!-- Mini FAB: Settings -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabSettings"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="22dp"
        android:layout_marginEnd="22dp"
        android:contentDescription="@string/fab_settings"
        android:src="@drawable/ic_settings"
        android:visibility="invisible"
        app:fabSize="mini"
        app:backgroundTint="@color/valueFriendship"
        app:tint="@color/white"
        app:elevation="6dp" />

    <!-- Mini FAB: Profile -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabProfile"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="22dp"
        android:layout_marginEnd="22dp"
        android:contentDescription="@string/fab_profile"
        android:src="@drawable/ic_person"
        android:visibility="invisible"
        app:fabSize="mini"
        app:backgroundTint="@color/valueSolidarity"
        app:tint="@color/white"
        app:elevation="6dp" />

    <!-- Main FAB -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabMain"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp"
        android:contentDescription="@string/fab_menu"
        android:src="@drawable/ic_menu"
        app:fabSize="normal"
        app:backgroundTint="@color/btnPrincipal"
        app:tint="@color/white"
        app:elevation="8dp" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>